<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.travel.mapper.OrderMapper">

    <resultMap id="BaseResultMap" type="com.travel.entity.Order">
        <id column="id" property="id"/>
        <result column="order_no" property="orderNo"/>
        <result column="user_id" property="userId"/>
        <result column="order_type" property="orderType"/>
        <result column="total_amount" property="totalAmount"/>
        <result column="discount_amount" property="discountAmount"/>
        <result column="pay_amount" property="payAmount"/>
        <result column="coupon_id" property="couponId"/>
        <result column="status" property="status"/>
        <result column="pay_time" property="payTime"/>
        <result column="pay_type" property="payType"/>
        <result column="pay_no" property="payNo"/>
        <result column="contact_name" property="contactName"/>
        <result column="contact_phone" property="contactPhone"/>
        <result column="remark" property="remark"/>
        <result column="create_time" property="createTime"/>
        <result column="update_time" property="updateTime"/>
    </resultMap>

    <sql id="Base_Column_List">
        id, order_no, user_id, order_type, total_amount, discount_amount, pay_amount,
        coupon_id, status, pay_time, pay_type, pay_no, contact_name, contact_phone,
        remark, create_time, update_time
    </sql>

    <!-- 根据ID查询 -->
    <select id="selectById" resultMap="BaseResultMap">
        SELECT
        <include refid="Base_Column_List"/>
        FROM `order`
        WHERE id = #{id}
    </select>

    <!-- 根据订单号查询 -->
    <select id="selectByOrderNo" resultMap="BaseResultMap">
        SELECT
        <include refid="Base_Column_List"/>
        FROM `order`
        WHERE order_no = #{orderNo}
    </select>

    <!-- 查询用户订单列表 -->
    <select id="selectByUserId" resultMap="BaseResultMap">
        SELECT
        <include refid="Base_Column_List"/>
        FROM `order`
        WHERE user_id = #{userId}
        <if test="status != null and status != ''">
            AND status = #{status}
        </if>
        ORDER BY create_time DESC
        <if test="offset != null and pageSize != null">
            LIMIT #{offset}, #{pageSize}
        </if>
    </select>

    <!-- 统计用户订单数量 -->
    <select id="countByUserId" resultType="long">
        SELECT COUNT(*)
        FROM `order`
        WHERE user_id = #{userId}
        <if test="status != null and status != ''">
            AND status = #{status}
        </if>
    </select>

    <!-- 插入订单 -->
    <insert id="insert" useGeneratedKeys="true" keyProperty="id">
        INSERT INTO `order` (
            order_no, user_id, order_type, total_amount, discount_amount, pay_amount,
            coupon_id, status, contact_name, contact_phone, remark
        ) VALUES (
            #{orderNo}, #{userId}, #{orderType}, #{totalAmount}, #{discountAmount}, #{payAmount},
            #{couponId}, #{status}, #{contactName}, #{contactPhone}, #{remark}
        )
    </insert>

    <!-- 更新订单 -->
    <update id="updateById">
        UPDATE `order`
        <set>
            <if test="orderNo != null">order_no = #{orderNo},</if>
            <if test="orderType != null">order_type = #{orderType},</if>
            <if test="totalAmount != null">total_amount = #{totalAmount},</if>
            <if test="discountAmount != null">discount_amount = #{discountAmount},</if>
            <if test="payAmount != null">pay_amount = #{payAmount},</if>
            <if test="couponId != null">coupon_id = #{couponId},</if>
            <if test="status != null">status = #{status},</if>
            <if test="payTime != null">pay_time = #{payTime},</if>
            <if test="payType != null">pay_type = #{payType},</if>
            <if test="payNo != null">pay_no = #{payNo},</if>
            <if test="contactName != null">contact_name = #{contactName},</if>
            <if test="contactPhone != null">contact_phone = #{contactPhone},</if>
            <if test="remark != null">remark = #{remark},</if>
        </set>
        WHERE id = #{id}
    </update>

    <!-- 更新订单状态 -->
    <update id="updateStatus">
        UPDATE `order`
        SET status = #{status}
        WHERE id = #{id}
    </update>

</mapper>
