<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.travel.mapper.ArticleImageMapper">

    <resultMap id="BaseResultMap" type="com.travel.entity.ArticleImage">
        <id column="id" property="id"/>
        <result column="article_id" property="articleId"/>
        <result column="image_url" property="imageUrl"/>
        <result column="sort" property="sort"/>
        <result column="create_time" property="createTime"/>
        <result column="update_time" property="updateTime"/>
    </resultMap>

    <sql id="Base_Column_List">
        id, article_id, image_url, sort, create_time, update_time
    </sql>

    <!-- 根据文章ID查询图片列表（按排序字段排序） -->
    <select id="selectByArticleId" resultMap="BaseResultMap">
        SELECT
        <include refid="Base_Column_List"/>
        FROM article_image
        WHERE article_id = #{articleId}
        ORDER BY sort ASC, create_time ASC
    </select>

    <!-- 根据ID查询图片 -->
    <select id="selectById" resultMap="BaseResultMap">
        SELECT
        <include refid="Base_Column_List"/>
        FROM article_image
        WHERE id = #{id}
    </select>

    <!-- 插入图片记录 -->
    <insert id="insert" useGeneratedKeys="true" keyProperty="id">
        INSERT INTO article_image (article_id, image_url, sort, create_time, update_time)
        VALUES (#{articleId}, #{imageUrl}, #{sort}, #{createTime}, #{updateTime})
    </insert>

    <!-- 根据ID删除图片记录 -->
    <delete id="deleteById">
        DELETE FROM article_image
        WHERE id = #{id}
    </delete>

    <!-- 根据文章ID删除所有图片记录 -->
    <delete id="deleteByArticleId">
        DELETE FROM article_image
        WHERE article_id = #{articleId}
    </delete>

    <!-- 更新图片排序 -->
    <update id="updateSort">
        UPDATE article_image
        SET sort = #{sort}, update_time = NOW()
        WHERE id = #{id}
    </update>

</mapper>
