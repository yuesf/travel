<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.travel.mapper.DirectoryMapper">

    <resultMap id="BaseResultMap" type="com.travel.entity.Directory">
        <id column="id" property="id"/>
        <result column="name" property="name"/>
        <result column="path" property="path"/>
        <result column="parent_id" property="parentId"/>
        <result column="level" property="level"/>
        <result column="sort" property="sort"/>
        <result column="created_at" property="createdAt"/>
        <result column="created_by" property="createdBy"/>
    </resultMap>

    <sql id="Base_Column_List">
        id, name, path, parent_id, level, sort, created_at, created_by
    </sql>

    <!-- 插入目录 -->
    <insert id="insert" parameterType="com.travel.entity.Directory" useGeneratedKeys="true" keyProperty="id">
        INSERT INTO directory (
            name,
            path,
            parent_id,
            level,
            sort,
            created_at,
            created_by
        ) VALUES (
            #{name},
            #{path},
            #{parentId},
            #{level},
            #{sort},
            #{createdAt},
            #{createdBy}
        )
    </insert>

    <!-- 根据ID查询 -->
    <select id="selectById" resultMap="BaseResultMap">
        SELECT
        <include refid="Base_Column_List"/>
        FROM directory
        WHERE id = #{id}
    </select>

    <!-- 根据路径查询 -->
    <select id="selectByPath" resultMap="BaseResultMap">
        SELECT
        <include refid="Base_Column_List"/>
        FROM directory
        WHERE path = #{path}
    </select>

    <!-- 查询所有目录 -->
    <select id="selectAll" resultMap="BaseResultMap">
        SELECT
        <include refid="Base_Column_List"/>
        FROM directory
        ORDER BY level ASC, sort ASC, id ASC
    </select>

    <!-- 根据父目录ID查询子目录列表 -->
    <select id="selectByParentId" resultMap="BaseResultMap">
        SELECT
        <include refid="Base_Column_List"/>
        FROM directory
        WHERE parent_id = #{parentId}
        ORDER BY sort ASC, id ASC
    </select>

    <!-- 删除目录 -->
    <delete id="delete">
        DELETE FROM directory
        WHERE id = #{id}
    </delete>

    <!-- 检查目录名称是否在同一父目录下已存在 -->
    <select id="countByNameAndParentId" resultType="int">
        SELECT COUNT(*)
        FROM directory
        WHERE name = #{name}
        AND (parent_id = #{parentId} OR (#{parentId} IS NULL AND parent_id IS NULL))
    </select>

</mapper>
