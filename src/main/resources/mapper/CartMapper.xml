<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.travel.mapper.CartMapper">

    <resultMap id="BaseResultMap" type="com.travel.entity.Cart">
        <id column="id" property="id"/>
        <result column="user_id" property="userId"/>
        <result column="item_type" property="itemType"/>
        <result column="item_id" property="itemId"/>
        <result column="quantity" property="quantity"/>
        <result column="create_time" property="createTime"/>
        <result column="update_time" property="updateTime"/>
    </resultMap>

    <sql id="Base_Column_List">
        id, user_id, item_type, item_id, quantity, create_time, update_time
    </sql>

    <!-- 根据ID查询 -->
    <select id="selectById" resultMap="BaseResultMap">
        SELECT
        <include refid="Base_Column_List"/>
        FROM cart
        WHERE id = #{id}
    </select>

    <!-- 根据用户ID查询购物车列表 -->
    <select id="selectByUserId" resultMap="BaseResultMap">
        SELECT
        <include refid="Base_Column_List"/>
        FROM cart
        WHERE user_id = #{userId}
        ORDER BY create_time DESC
    </select>

    <!-- 根据用户ID和商品信息查询购物车 -->
    <select id="selectByUserAndItem" resultMap="BaseResultMap">
        SELECT
        <include refid="Base_Column_List"/>
        FROM cart
        WHERE user_id = #{userId}
          AND item_type = #{itemType}
          AND item_id = #{itemId}
        LIMIT 1
    </select>

    <!-- 插入购物车 -->
    <insert id="insert" useGeneratedKeys="true" keyProperty="id">
        INSERT INTO cart (
            user_id, item_type, item_id, quantity
        ) VALUES (
            #{userId}, #{itemType}, #{itemId}, #{quantity}
        )
    </insert>

    <!-- 更新购物车 -->
    <update id="updateById">
        UPDATE cart
        <set>
            <if test="quantity != null">quantity = #{quantity},</if>
        </set>
        WHERE id = #{id}
    </update>

    <!-- 删除购物车 -->
    <delete id="deleteById">
        DELETE FROM cart WHERE id = #{id}
    </delete>

    <!-- 根据用户ID删除所有购物车 -->
    <delete id="deleteByUserId">
        DELETE FROM cart WHERE user_id = #{userId}
    </delete>

</mapper>
