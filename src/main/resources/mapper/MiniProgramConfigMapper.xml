<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.travel.mapper.MiniProgramConfigMapper">

    <resultMap id="BaseResultMap" type="com.travel.entity.MiniProgramConfig">
        <id column="id" property="id"/>
        <result column="config_key" property="configKey"/>
        <result column="config_value" property="configValue"/>
        <result column="config_type" property="configType"/>
        <result column="description" property="description"/>
        <result column="sort" property="sort"/>
        <result column="status" property="status"/>
        <result column="create_time" property="createTime"/>
        <result column="update_time" property="updateTime"/>
    </resultMap>

    <sql id="Base_Column_List">
        id, config_key, config_value, config_type, description, sort, status, create_time, update_time
    </sql>

    <!-- 根据ID查询 -->
    <select id="selectById" resultMap="BaseResultMap">
        SELECT
        <include refid="Base_Column_List"/>
        FROM miniprogram_config
        WHERE id = #{id}
    </select>

    <!-- 根据配置键查询 -->
    <select id="selectByConfigKey" resultMap="BaseResultMap">
        SELECT
        <include refid="Base_Column_List"/>
        FROM miniprogram_config
        WHERE config_key = #{configKey}
    </select>

    <!-- 根据配置类型查询配置列表 -->
    <select id="selectByConfigType" resultMap="BaseResultMap">
        SELECT
        <include refid="Base_Column_List"/>
        FROM miniprogram_config
        <where>
            <if test="configType != null and configType != ''">
                AND config_type = #{configType}
            </if>
            <if test="status != null">
                AND status = #{status}
            </if>
        </where>
        ORDER BY sort ASC, create_time DESC
    </select>

    <!-- 查询所有配置列表 -->
    <select id="selectAll" resultMap="BaseResultMap">
        SELECT
        <include refid="Base_Column_List"/>
        FROM miniprogram_config
        <where>
            <if test="configType != null and configType != ''">
                AND config_type = #{configType}
            </if>
            <if test="status != null">
                AND status = #{status}
            </if>
        </where>
        ORDER BY sort ASC, create_time DESC
    </select>

    <!-- 插入配置 -->
    <insert id="insert" useGeneratedKeys="true" keyProperty="id">
        INSERT INTO miniprogram_config (
            config_key, config_value, config_type, description, sort, status
        ) VALUES (
            #{configKey}, #{configValue}, #{configType}, #{description}, #{sort}, #{status}
        )
    </insert>

    <!-- 更新配置信息 -->
    <update id="updateById">
        UPDATE miniprogram_config
        <set>
            <if test="configKey != null">config_key = #{configKey},</if>
            <if test="configValue != null">config_value = #{configValue},</if>
            <if test="configType != null">config_type = #{configType},</if>
            <if test="description != null">description = #{description},</if>
            <if test="sort != null">sort = #{sort},</if>
            <if test="status != null">status = #{status},</if>
        </set>
        WHERE id = #{id}
    </update>

    <!-- 删除配置（物理删除） -->
    <delete id="deleteById">
        DELETE FROM miniprogram_config WHERE id = #{id}
    </delete>

    <!-- 根据配置键删除配置 -->
    <delete id="deleteByConfigKey">
        DELETE FROM miniprogram_config WHERE config_key = #{configKey}
    </delete>

</mapper>
