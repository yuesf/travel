<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.travel.mapper.OssConfigMapper">

    <resultMap id="BaseResultMap" type="com.travel.entity.OssConfig">
        <id column="id" property="id"/>
        <result column="endpoint" property="endpoint"/>
        <result column="access_key_id" property="accessKeyId"/>
        <result column="access_key_secret" property="accessKeySecret"/>
        <result column="bucket_name" property="bucketName"/>
        <result column="enabled" property="enabled"/>
        <result column="created_at" property="createdAt"/>
        <result column="updated_at" property="updatedAt"/>
        <result column="created_by" property="createdBy"/>
        <result column="updated_by" property="updatedBy"/>
    </resultMap>

    <sql id="Base_Column_List">
        id, endpoint, access_key_id, access_key_secret, bucket_name, enabled,
        created_at, updated_at, created_by, updated_by
    </sql>

    <!-- 查询OSS配置（单例） -->
    <select id="selectOne" resultMap="BaseResultMap">
        SELECT
        <include refid="Base_Column_List"/>
        FROM oss_config
        LIMIT 1
    </select>

    <!-- 插入OSS配置 -->
    <insert id="insert" parameterType="com.travel.entity.OssConfig" useGeneratedKeys="true" keyProperty="id">
        INSERT INTO oss_config (
            endpoint,
            access_key_id,
            access_key_secret,
            bucket_name,
            enabled,
            created_at,
            updated_at,
            created_by,
            updated_by
        ) VALUES (
            #{endpoint},
            #{accessKeyId},
            #{accessKeySecret},
            #{bucketName},
            #{enabled},
            #{createdAt},
            #{updatedAt},
            #{createdBy},
            #{updatedBy}
        )
    </insert>

    <!-- 更新OSS配置 -->
    <update id="update" parameterType="com.travel.entity.OssConfig">
        UPDATE oss_config
        SET endpoint = #{endpoint},
            access_key_id = #{accessKeyId},
            access_key_secret = #{accessKeySecret},
            bucket_name = #{bucketName},
            enabled = #{enabled},
            updated_at = #{updatedAt},
            updated_by = #{updatedBy}
        WHERE id = #{id}
    </update>

    <!-- 删除OSS配置 -->
    <delete id="delete">
        DELETE FROM oss_config
    </delete>

</mapper>
